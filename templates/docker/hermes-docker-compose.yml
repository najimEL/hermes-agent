services:
  hermes-agent:
    image: nousresearch/hermes-agent:latest
    container_name: ${HERMES_AGENT_CONTAINER_NAME:-hermes-agent}
    restart: unless-stopped
    command: ["gateway", "run"]
    env_file:
      - $PROJECT_DIR/.env
    environment:
      API_SERVER_KEY: ${API_SERVER_KEY:-}
      DASHBOARD_MODE: 1
    ports:
      - "${HERMES_API_PORT:-8642}:8642"   # gateway API
      - "${HERMES_DASHBOARD_PORT:-9119}:9119"   # dashboard
    volumes:
      - $PROJECT_DIR/.hermes:/opt/data
      - $PROJECT_DIR/docker-service:/opt/hermes/docker-service
      - $PROJECT_DIR/templates:/opt/hermes/templates
      - $PROJECT_DIR/backups:/opt/hermes/backups
    deploy:
      resources:
        limits:
          memory: 4G
          cpus: "2.0"

  hermes-dashboard:
    image: ghcr.io/outsourc-e/hermes-workspace:latest
    container_name: ${HERMES_WORKSPACE_CONTAINER_NAME:-hermes-dashboard}
    restart: unless-stopped
    env_file:
      - $PROJECT_DIR/.env
    environment:
      HERMES_API_URL: http://hermes-agent:8642
      HERMES_DASHBOARD_URL: http://hermes-agent:9119
      HERMES_API_TOKEN: ${API_SERVER_KEY:-}
    ports:
      - "${HERMES_WORKSPACE_WEB_PORT:-3000}:3000"
    depends_on:
      - hermes-agent
